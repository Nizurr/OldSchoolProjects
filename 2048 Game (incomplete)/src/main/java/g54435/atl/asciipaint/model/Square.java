package g54435.atl.asciipaint.model;

public class Square extends Rectangle {

    /**
     * Constructor of the Square
     * @param upperLeft Point of the upperLeft (position of the Square)
     * @param width width of the Square
     * @param height height of the Square
     * @param color color in
     */
    public Square(Point upperLeft, double width, double height, char color) {
        super(upperLeft, width, height, color);
    }
}
